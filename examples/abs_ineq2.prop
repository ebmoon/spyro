//@Description Toy benchmarks to show complex recursive generators.

var {
    int x;
    int o;
}

relation {
    abs(x, o);
}

generator {
    boolean AP -> I > ??(4) | I > -??(4) ;
    int I -> C * x + C * o + C ;
    int C -> ??(3) - 4;
}

example {
    int -> ?? | -1 * ?? ;
}

void abs(int x, ref int out){
    if (x < 0) {
        out = -x;
    } else {
        out = x;
    }
}