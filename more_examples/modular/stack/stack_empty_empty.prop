//@Description Sketch to reverse a list.

var {
    stack empty_out;

    boolean is_empty_out;

    boolean err;
}

relation {
    empty(empty_out);
    is_empty(empty_out, is_empty_out);
}

generator {
    boolean AP -> !GUARD || RHS;
    boolean GUARD -> true;
    boolean RHS -> is_empty_out == BB;
    boolean BB -> true | false;
}

example {
    int -> ??(3) | -1 * ??(3) ;
    boolean -> true | false;
    stack -> empty() | push(stack, int);
}