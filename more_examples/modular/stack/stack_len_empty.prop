//@Description Sketch to reverse a list.

var {
    stack empty_out;

    int len_out;
}

relation {
    empty(empty_out);
    stack_len(empty_out, len_out);
}

generator {
    boolean AP -> !GUARD || RHS;
    boolean GUARD -> true;
    boolean RHS -> len_out == ??;
}

example {
    int -> ??(3) | -1 * ??(3) ;
    boolean -> true | false;
    stack -> empty() | push(stack, int);
}